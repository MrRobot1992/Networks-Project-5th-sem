<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search Results</title>
  <style>
    body {
      font-family: 'Arial', sans-serif; /* You can replace 'Arial' with another font */
      background-image: url("/background3.jpg"); /* Correct path for background */
      background-size: cover;
      background-repeat: no-repeat;
      height: 100vh;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      color: #fff;
    }
    .container {
      text-align: center;
      background: rgba(0, 0, 0, 0.7); /* Semi-transparent black background */
      padding: 20px;
      border-radius: 10px;
      max-width: 500px;
      width: 90%;
    }
    h1 {
      font-size: 2rem;
      margin-bottom: 20px;
    }
    p {
      font-size: 1rem;
    }
    ul {
      list-style: none;
      padding: 0;
    }
    li {
      margin: 10px 0;
      font-size: 1.6rem; /* Increase font size by 60% */
    }
    a {
      color: #00d1b2; /* Nice teal color */
      text-decoration: none;
      font-weight: bold;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Search Results for "<%= searchQuery %>"</h1>
    <% if (message) { %>
      <p><%= message %></p>
    <% } else { %>
      <ul>
        <% results.forEach(result => { %>
          <li>
            <a href="/<%= result.name.toLowerCase() %>"><%= result.name %></a>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </div>
</body>
</html>
